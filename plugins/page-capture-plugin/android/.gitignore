*.iml
.gradle
build
.DS_Store
.externalNativeBuild
.cxx
local.properties

# Regenerated by `mobile::setup` (this crate's build.rs) from Tauri's own
# Android library on every Android build — see `render_android`'s docs.
/.tauri
